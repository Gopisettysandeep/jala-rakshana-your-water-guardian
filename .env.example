# Copy this file to `.env` and fill in the values.
# Only needed for the AI Assistant and AI climate advisories.
# Everything else (weather, schemes, quiz, community, media) works without it.

LOVABLE_API_KEY=
